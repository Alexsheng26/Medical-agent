[project]
name = "medical-research-agent"
version = "0.4.1"
description = "Research intermediary agent for clinical and translational medicine"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.120",
    "pypdf>=4.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
# Optional: lets tests/test_webui.py drive the page in a real browser. Without
# it those six tests skip and the rest still run.
browser = ["playwright>=1.40"]
# For pointing the tool at an OpenAI-compatible endpoint (DeepSeek and friends).
openai = ["openai>=1.40"]

[project.scripts]
mra = "mra.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["mra*"]

[tool.setuptools.package-data]
# examples ship with the package: without them `mra demo` and every "try it
# first" instruction only work for someone who cloned the repository.
# assets/ holds the web interface, read through importlib.resources.
mra = ["prompts/*.md", "examples/*", "assets/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
